sysrescue-testbench.srm
systemrescue-12.03-testbench-amd64.iso.sha256
systemrescue-persist.dat
systemrescue-12.03-amd64.iso

*.iso
*.srm
*.apk
*.tar.gz
*.tcz
*.tcz.*

*.fd
*.qcow2
*.img
*.iso.sha256

.boot_repository

autoinstall-usb/work

# tcz-cache/ is entirely generated by autoinstall-usb/tcz/fetch-tcz.sh from
# the single canonical autoinstall-usb/tcz/onboot.lst; nothing under it
# (including its own onboot.lst/manifest.json copies) is source-of-truth
# and none of it should ever be committed (agent-plan Phase 1: "Declare
# tcz/onboot.lst as the single canonical package list").
autoinstall-usb/tcz-cache/

.vagrant/

# Phase 0: developer tooling / test harness artifacts
tests/tmp/
tests/**/*.log
.cache/
node_modules/
*.bats.log
build-manifest.json
